/** Default cache TTL for resolver results. */
export const DEFAULT_RESOLVER_TTL_MS = 60_000;
/** Default per-lookup timeout. */
export const DEFAULT_RESOLVER_TIMEOUT_MS = 50;
/** Default per-logger enrichment cache capacity. */
export const DEFAULT_RESOLVER_CACHE_CAP = 8192;
